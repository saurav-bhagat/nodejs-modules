/**
 * 1. How to create a new file?
 * 2. How to add content to this newly created file?
 * 3. How to append some content to already existing file?
 * 4. How to read the data inside the file?
 * 5. What is buffer data?
 * 6. How to convert buffer data into a readable format?
 * 7. How to delete file?
 */

const fs = require('fs');

// fs.writeFileSync('data.txt', 'We are learning nodejs today.');
// fs.appendFileSync('data.txt', " We are the students of nia batch, 2022");
// const buffer_data = fs.readFileSync('data.txt').toString();
// console.log(buffer_data);

// fs.mkdirSync('utils');
// fs.writeFileSync('utils/data.txt', 'We are learning nodejs today.');

// const data = fs.readFileSync('utils/data.txt').toString();

// console.log(data);

// fs.renameSync('utils/myData.html', 'utils/myData.txt');

// fs.appendFileSync('utils/myData.txt', " We are students of nia batch, 2022");

// fs.unlinkSync('utils/myData.txt');

// fs.rmdirSync('utils');